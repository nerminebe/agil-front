export class ProduitStation {
    id: number;         // Maps to Long id
    produitId: number; // Assuming you have a `produit` with an ID
    stationId: number; // Assuming you have a `station` with an ID
    qte: number;       // Maps to Long qte
  }
  